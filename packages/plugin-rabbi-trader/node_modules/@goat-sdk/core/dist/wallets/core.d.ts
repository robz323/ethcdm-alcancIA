type Signature = {
    signature: string;
};
type Balance = {
    decimals: number;
    symbol: string;
    name: string;
    value: bigint;
};
/**
 * @param type - "evm" or "solana", extend this union as needed (e.g., "sui")
 * @param id - Chain ID, optional for EVM
 */
type Chain = {
    type: "evm" | "solana";
    id?: number;
};
type EVMChain = Chain & {
    type: "evm";
};
type SolanaChain = Chain & {
    type: "solana";
};
interface WalletClient {
    getAddress: () => string;
    getChain: () => Chain;
    signMessage: (message: string) => Promise<Signature>;
    balanceOf: (address: string) => Promise<Balance>;
}

export type { Balance, Chain, EVMChain, Signature, SolanaChain, WalletClient };
