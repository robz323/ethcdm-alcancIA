import {
  balanceOf,
  transfer
} from "./chunk-T3IAETW7.mjs";
import {
  getBalanceParametersSchema,
  transferParametersSchema
} from "./chunk-RSFQWU6B.mjs";

// src/tools.ts
function getTools(tokenList) {
  const tools = [];
  for (const token of tokenList) {
    const balanceTool = {
      name: `get_${token.symbol}_balance`,
      description: `This {{tool}} gets the balance of ${token.symbol}`,
      parameters: getBalanceParametersSchema,
      method: (walletClient, parameters) => balanceOf(walletClient, token, parameters)
    };
    const transferTool = {
      name: `transfer_${token.symbol}`,
      description: `This {{tool}} transfers ${token.symbol} to the specified address`,
      parameters: transferParametersSchema,
      method: (walletClient, parameters) => transfer(walletClient, token, parameters)
    };
    tools.push(balanceTool, transferTool);
  }
  return tools;
}

export {
  getTools
};
//# sourceMappingURL=chunk-T5MUU4IS.mjs.map