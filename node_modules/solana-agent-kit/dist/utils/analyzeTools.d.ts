import { TiktokenModel } from "tiktoken";
interface ToolAnalysis {
    name: string;
    actionTokens: number;
    langchainTokens: number;
    vercelTokens: number;
    totalTokens: number;
}
export declare function analyzeToolTokens(modelName?: TiktokenModel, includeLangchain?: boolean): Promise<ToolAnalysis[]>;
export {};
//# sourceMappingURL=analyzeTools.d.ts.map