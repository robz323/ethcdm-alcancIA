"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getDebridgeSupportedChains = getDebridgeSupportedChains;
const constants_1 = require("../../constants");
/**
 * Get list of chains supported by deBridge protocol
 * @returns List of supported chains with their configurations
 * @throws {Error} If the API request fails or returns an error
 */
async function getDebridgeSupportedChains() {
    const response = await fetch(`${constants_1.DEBRIDGE_API}/supported-chains-info`);
    if (!response.ok) {
        throw new Error(`Failed to fetch supported chains: ${response.statusText}`);
    }
    const data = await response.json();
    if ("error" in data) {
        throw new Error(`API Error: ${data.error}`);
    }
    return data;
}
//# sourceMappingURL=getSupportedChains.js.map