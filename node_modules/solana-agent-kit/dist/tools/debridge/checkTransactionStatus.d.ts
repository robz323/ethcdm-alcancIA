import { SolanaAgentKit } from "../../agent";
import { deBridgeOrderStatusResponse } from "../../types";
/**
 * Check the status of a bridge transaction using its transaction hash
 * @param agent SolanaAgentKit instance
 * @param txHash Transaction hash to check status for
 * @returns Status information for the bridge transaction
 * @throws {Error} If the API request fails or returns an error
 */
export declare function checkDebridgeTransactionStatus(agent: SolanaAgentKit, txHash: string): Promise<deBridgeOrderStatusResponse[]>;
//# sourceMappingURL=checkTransactionStatus.d.ts.map