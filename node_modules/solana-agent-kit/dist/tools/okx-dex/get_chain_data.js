"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getChainData = getChainData;
const utils_1 = require("./utils");
/**
 * Get chain data from OKX DEX
 * @param agent SolanaAgentKit instance
 * @returns Chain data from OKX DEX
 */
async function getChainData(agent) {
    try {
        const dexClient = (0, utils_1.initDexClient)(agent);
        const chains = await dexClient.dex.getChainData("501");
        return chains;
    }
    catch (error) {
        return {
            status: "error",
            message: error.message || "Failed to get chain data",
        };
    }
}
//# sourceMappingURL=get_chain_data.js.map