import { TxResponse } from '../types/index.js';
export declare const waitTxBroadcasted: (txHash: string, options: {
    endpoints: {
        grpc?: string;
        rest: string;
    };
    txTimeout?: number;
}) => Promise<TxResponse>;
