/**
 * THIS FEATURE IS EXPERIMENTAL AND IN BETA, USE AT YOUR OWN RISK
 */
import { AnonProxyConfig } from './config/config';
/**
 * Allows to run Anon Proxy with different configuration options
 */
export declare class AnonProxy {
    private options?;
    private process?;
    constructor(options?: AnonProxyConfig);
    /**
     * Starts Anon Proxy
     */
    start(args: string[]): Promise<void>;
    /**
     * Stops Anon Proxy
     */
    stop(): Promise<void>;
    /**
     * Allows to check if Anon Proxy is running
     * @returns true if Anon Proxy is running
     */
    isRunning(): boolean;
    private onStop;
    private runBinary;
}
