export interface AnonConfig {
    displayLog?: boolean;
    useExecFile?: boolean;
    socksPort: number;
    orPort: number;
    controlPort: number;
    configFile?: string;
    binaryPath?: string;
    autoTermsAgreement?: boolean;
}
export declare function createAnonConfigFile(options: AnonConfig): Promise<string>;
export interface AnonProxyConfig {
    socksPort?: number;
}
export declare function createAnonProxyConfigFile(options?: AnonProxyConfig): Promise<string>;
