import { UTXOActions } from '@bigmi/core';
import type { UTXOSchema } from '@bigmi/core';
import { type Chain, type Client, type FallbackTransport, type HttpTransport } from 'viem';
/**
 * Get an instance of a provider for a specific chain
 * @param chainId - Id of the chain the provider is for
 * @returns The public client for the given chain
 */
export declare const getUTXOPublicClient: (chainId: number) => Promise<Client<FallbackTransport<readonly HttpTransport[]>, Chain, undefined, UTXOSchema, UTXOActions>>;
//# sourceMappingURL=getUTXOPublicClient.d.ts.map