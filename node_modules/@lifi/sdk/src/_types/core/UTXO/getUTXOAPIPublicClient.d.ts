import { UTXOAPIActions } from '@bigmi/core';
import type { UTXOAPISchema } from '@bigmi/core';
import { type Chain, type Client, type FallbackTransport, type HttpTransport } from 'viem';
/**
 * Get an instance of a provider for a specific chain
 * @param chainId - Id of the chain the provider is for
 * @returns The public client for the given chain
 */
export declare const getUTXOAPIPublicClient: (chainId: number) => Promise<Client<FallbackTransport<readonly HttpTransport[]>, Chain, undefined, UTXOAPISchema, UTXOAPIActions>>;
//# sourceMappingURL=getUTXOAPIPublicClient.d.ts.map