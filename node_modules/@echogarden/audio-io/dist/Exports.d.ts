export { playAudioSamples, playTestTone } from './Player.js';
export declare function createAudioOutput(config: AudioOutputConfig, handler: AudioOutputHandler): Promise<AudioOutput>;
export declare function isPlatformSupported(): boolean;
export interface AudioOutput {
    dispose(): Promise<void>;
}
export type AudioOutputHandler = (outputBuffer: Int16Array) => void;
export interface AudioOutputConfig {
    sampleRate: number;
    channelCount: number;
    bufferDuration?: number;
}
