// src/solana/methods.ts
import { formatUnits } from "viem";
function getAddress(walletClient, parameters) {
  return walletClient.getAddress();
}
async function getBalance(walletClient, parameters) {
  try {
    const balance = await walletClient.balanceOf(
      parameters.address ?? getAddress(walletClient, {})
    );
    return formatUnits(balance.value, balance.decimals);
  } catch (error) {
    throw new Error(`Failed to fetch balance: ${error}`);
  }
}

export {
  getAddress,
  getBalance
};
//# sourceMappingURL=chunk-X3GUUTRZ.mjs.map