"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.tryGetNftCollName = exports.tryGetNftShortName = void 0;
// th, st, nd, rd is for degenfatcats
const _nameIndexRegex = new RegExp('(\\d+)(th|st|nd|rd)?$');
const tryGetNftShortName = (name) => {
    const shortName = _nameIndexRegex.exec(name);
    if (shortName?.length) {
        return '#' + shortName[1];
    }
    return name;
};
exports.tryGetNftShortName = tryGetNftShortName;
const tryGetNftCollName = (name) => {
    // const shortName = name!.matchAll(/(\d+)(th)?$/g);
    const shortName = _nameIndexRegex.exec(name);
    if (shortName?.length) {
        const suffix = shortName[1].length;
        return name.slice(0, -suffix).replaceAll('#', '');
    }
    return name;
};
exports.tryGetNftCollName = tryGetNftCollName;
//# sourceMappingURL=nft.js.map