import { LAMPORTS_PER_SOL } from '@solana/web3.js';
import axios from 'axios';
import Big from 'big.js';
import { ME_AH_ADDRESS, ME_URL, makeMEHeaders } from './shared';
export const makeMEDelistTx = async ({ tokenMint, tokenOwner, tokenAccount, priceLamports, apiKey, expiry, }) => {
    const price = new Big(priceLamports).div(LAMPORTS_PER_SOL).toNumber();
    const { data } = await axios.get(`${ME_URL}/v2/instructions/sell_cancel`, {
        params: {
            seller: tokenOwner,
            auctionHouseAddress: ME_AH_ADDRESS,
            tokenMint,
            tokenAccount,
            price,
            expiry,
        },
        headers: makeMEHeaders(apiKey),
    });
    return data.txSigned.data;
};
//# sourceMappingURL=delist.js.map