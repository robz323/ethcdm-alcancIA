export const isImageFile = (filename) => ['.jpg', '.jpeg', '.png', '.gif', '.bmp'].includes(filename.substring(filename.lastIndexOf('.')).toLowerCase());
export const isVideoFile = (filename) => ['.mp4', '.avi', '.mov', '.mkv'].includes(filename.substring(filename.lastIndexOf('.')).toLowerCase());
export const isGifFile = (filename) => ['.gif'].includes(filename.substring(filename.lastIndexOf('.')).toLowerCase());
export const isImageOrVideoOrGifFile = (filename) => isImageFile(filename) || isVideoFile(filename) || isGifFile(filename);
/** returns true if content can't change for a given url */
export const hasFixedContentAddress = (uri) => {
    // NB: Shadow Drive is NOT content addressable.
    const fixedSources = [
        'nftstorage.link',
        'arweave.net',
        'mypinata.cloud',
        'dweb.link',
        'ipfs.io',
    ];
    return fixedSources.some((s) => uri.includes(s));
};
//# sourceMappingURL=files.js.map