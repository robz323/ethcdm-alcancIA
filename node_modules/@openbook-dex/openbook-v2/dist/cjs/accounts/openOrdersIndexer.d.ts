import { PublicKey } from '@solana/web3.js';
import { OpenBookV2Client, OpenOrdersIndexerAccount, OpenOrders } from '..';
export declare class OpenOrdersIndexer {
    client: OpenBookV2Client;
    pubkey: PublicKey;
    account: OpenOrdersIndexerAccount;
    constructor(client: OpenBookV2Client, pubkey: PublicKey, account: OpenOrdersIndexerAccount);
    static load(client: OpenBookV2Client, owner?: PublicKey): Promise<OpenOrdersIndexer>;
    static loadNullable(client: OpenBookV2Client, owner?: PublicKey): Promise<OpenOrdersIndexer | null>;
    loadAllOpenOrders(): Promise<OpenOrders[]>;
}
