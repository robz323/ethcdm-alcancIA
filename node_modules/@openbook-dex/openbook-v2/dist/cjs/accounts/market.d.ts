/// <reference types="bn.js" />
import Big from 'big.js';
import { BN } from '@coral-xyz/anchor';
import { PublicKey } from '@solana/web3.js';
import { MarketAccount, OpenBookV2Client, BookSide, EventHeap } from '..';
export declare class Market {
    client: OpenBookV2Client;
    pubkey: PublicKey;
    account: MarketAccount;
    minOrderSize: Big;
    tickSize: Big;
    quoteLotFactor: Big;
    baseNativeFactor: Big;
    quoteNativeFactor: Big;
    /**
     * use async loadBids() or loadOrderBook() to populate bids
     */
    bids: BookSide | undefined;
    /**
     * use async loadAsks() or loadOrderBook() to populate asks
     */
    asks: BookSide | undefined;
    eventHeap: EventHeap | undefined;
    constructor(client: OpenBookV2Client, pubkey: PublicKey, account: MarketAccount);
    static load(client: OpenBookV2Client, pubkey: PublicKey): Promise<Market>;
    baseLotsToUi(lots: BN): number;
    baseNativeToUi(native: BN): number;
    quoteLotsToUi(lots: BN): number;
    quoteNativeToUi(native: BN): number;
    priceLotsToUi(lots: BN): number;
    baseUiToLots(uiAmount: number): BN;
    quoteUiToLots(uiAmount: number): BN;
    priceUiToLots(uiAmount: number): BN;
    makerFeeFloor(quoteNative: BN): BN;
    loadBids(): Promise<BookSide>;
    loadAsks(): Promise<BookSide>;
    loadEventHeap(): Promise<EventHeap>;
    loadOrderBook(): Promise<this>;
    toPrettyString(): string;
}
